- 所有 Tauri 命令函数统一放在 `commands` 子模块中，并通过 `generate_handler!` 宏集中注册到 `Builder`，避免分散声明。
- 应用级错误集中在 `error.rs` 中以 `enum AppError` 形式表达，使用 `thiserror` 派生 Display/From，并手动实现 `Serialize` 将错误序列化为字符串传给前端。
- 二进制入口 `main.rs` 保持极简，仅委托给 `hello_tauri::run()`，业务初始化全部收敛在 `lib.rs` 的 `run()` 中。