- 所有对外暴露的 struct 都加上 `#[derive(serde::Serialize)]` 与 `#[serde(rename_all = "camelCase")]`，确保字段名以 camelCase 传给前端。
- 命令函数统一返回 `Result<T, AppError>`，并通过 `?` 或 `map_err(AppError::Io)` 将底层 io 错误归一化到 `AppError`。
- 对路径参数做前置安全检查（如拒绝包含 `..`），防止路径穿越攻击后再交给底层库处理。