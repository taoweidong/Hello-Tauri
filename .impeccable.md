# Design Context — Hello-Tauri

> impeccable 设计上下文。首次由 2026-09-24 界面优化任务确立。

## Audience（谁在用）

内网环境的工程师 / 运维人员，Windows 桌面端（Tauri + WebView2），长时间会话，
窗口 1180×760（最小 900×600）。网络隔离，任何外部资源都加载不到。

## Use cases（要完成什么）

- 浏览与增删改查业务记录（搜索、分类筛选、分页、批量删除）
- 调整应用配置（主题、分页、默认页、自动保存）并确认存储位置
- 快速掌握运行状态（版本、平台、配置文件路径、日志去向）

## Tone（应该有什么感觉）

**精致控制台风**：深色侧栏轨道 + 浅色画布（Linear / Raycast 谱系）。
单一钢靛主色、hairline 分割线代替阴影、tabular 数字、克制的 150–220ms 过渡。
专业、安静、可信——密度是特性，装饰是缺陷。

## Constraints（硬约束）

- **零 CDN**：不引任何在线字体 / 图标 / 脚本；仅系统字体栈
  （Segoe UI / Microsoft YaHei）+ 本地内联 SVG
- Element Plus 保留，但主题通过 CSS 变量整体覆写；亮/暗双主题都要稳定
- 动效只作用于 opacity/transform，遵守 prefers-reduced-motion
- 业务逻辑不动：只改表现层（views / layouts / styles / components）

## Differentiation（记忆点）

深轨道 + 亮画布的双层结构；全部数字走等宽 tabular 对齐；
分类色带只出现在数据表达处（堆叠条/头像点），其他地方保持中性。

## The AI Slop Test

不出现：紫蓝渐变、青+暗黑霓虹、居中英雄区大数字模板、卡片套卡片。
如果一眼"像 AI 做的"，重做。